VERIFIED

## Execution Summary

- **Instance Name:** `instance1`
- **Resource Prefix:** `substrat-instance1`
- **GCP Project:** `barni-cnrm-20260529` (Project Number: `77658989016`)
- **GKE Cluster:** `substrat-instance1` (`us-central1-a`, GKE `v1.36.4-gke.1391000`, 2 nodes `c3-standard-4`)
- **GCS Snapshot Bucket:** `gs://substrat-instance1-snap-77658989016`
- **Container Registry:** `gcr.io/barni-cnrm-20260529/substrat-instance1`
- **Resource Labels:** `repo-agent-instance=substrat-instance1`

---

## Attempt Log

### Attempt 1
- **Action:** Executed `bash docs-exploration/runbook-deployments/instance1/deploy.sh`.
- **Event:** GKE cluster creation operation (`operation-1790226077928-b816a60e-2c6d-497e-b9ef-f2ec5390db61`) took ~7 minutes on GCP. The tool caller hit its command execution timeout while waiting for stdout.
- **Diagnosis:** Confirmed read-only via `gcloud container operations list` that the cluster creation operation was actively progressing and completed (`status: DONE`).
- **Correction:** Exported `$(go env GOPATH)/bin` in `deploy.sh` so `kubectl-ate` is immediately on `PATH`. Re-ran `deploy.sh` which idempotently verified cluster attributes, created the GCS snapshot bucket with Uniform Bucket-Level Access, configured IAM policy bindings, updated resource labels, disabled node pool auto-upgrades, deployed the Substrate control plane (`hack/install-ate.sh --deploy-ate-system`), deployed the counter demo (`hack/install-ate.sh --deploy-demo-counter`), and installed `kubectl-ate`. All steps succeeded cleanly with exit code 0.

---

## Verification Evidence

1. **Control Plane & Pod Health:**
   - `kubectl get pods -n ate-system`:
     - `ate-api-server` (2 replicas: 1/1 Running)
     - `ate-controller` (1 replica: 1/1 Running)
     - `atelet` DaemonSet (2 replicas: 1/1 Running on both `c3-standard-4` nodes)
     - `atenet-egress` (1 replica: 2/2 Running)
     - `atenet-router` (1 replica: 2/2 Running)
     - `postgres-0` StatefulSet (1 replica: 2/2 Running with bound PVC)
     - `podcertificate-controller` (1 replica: 1/1 Running)
   - `kubectl get nodes -L ate.dev/substrate-version`:
     - Both nodes `Ready` and labeled with `ate.dev/substrate-version=89c1866c`.
   - `kubectl get clustertrustbundles`:
     - `podidentity.podcert.ate.dev:identity:primary-bundle`
     - `servicedns.podcert.ate.dev:identity:primary-bundle`
     - `kubernetes.io:kube-apiserver-serving:...`
   - `kubectl get workerpools -A`:
     - `ate-demo-counter/counter` (Desired: 3, Ready: 3).
   - `kubectl ate get actor-templates -a ate-demo-counter`:
     - `counter` with golden tag `4bb74369-a42f-44a4-a5af-8e5f8f516f42`.

2. **Interactive Round-Trip & Snapshot Verification:**
   - Created actor `rb-counter-1` (`kubectl ate create actor rb-counter-1 -a ate-demo-counter --template counter`).
   - Port-forwarded `svc/atenet-router` port 8000:80.
   - Sent HTTP POST: `hello from: 169.254.17.2 | preserved memory count: 1 | preserved file counter: 1`.
   - Actor transitioned to `ACTOR_STATE_RUNNING` on worker pod `counter-7984775f76-nzhx9`.
   - Suspended actor (`kubectl ate suspend actor rb-counter-1 -a ate-demo-counter`).
   - Verified snapshot artifacts in `gs://substrat-instance1-snap-77658989016/`:
     - `checkpoint.img.zstd`
     - `durable-dir.tar.zstd`
     - `manifest.json`
     - `pages.img.zstd`
     - `pages_meta.img.zstd`
   - Sent second HTTP POST: `hello from: 169.254.17.2 | preserved memory count: 2 | preserved file counter: 2`. Counter resumed state from GCS snapshot accurately.
   - Suspended and deleted actor cleanly.

3. **End-to-End Suite Verification:**
   - Ran `hack/run-e2e.sh ./internal/e2e/suites/demo -run '^TestActorLifecycle$' -v -args --no-color`.
   - All 6 subtests passed in 15.356s:
     - `TestActorLifecycle/CreateActor` (PASS)
     - `TestActorLifecycle/SuspendResumeActor` (PASS)
     - `TestActorLifecycle/PauseResumeActor` (PASS)
     - `TestActorLifecycle/RevertResumeActor` (PASS)
     - `TestActorLifecycle/DeletePausedActorAnyState` (PASS)
     - `TestActorLifecycle/DeleteActorAnyState` (PASS)

---

## What is Left Running (Teardown Contract)

- **GKE Cluster:** `substrat-instance1` (location `us-central1-a`, 2x `c3-standard-4` VMs)
- **GCS Bucket:** `gs://substrat-instance1-snap-77658989016` (location `us-central1`)
- **Container Images:** `gcr.io/barni-cnrm-20260529/substrat-instance1/*`
- **Persistent Disk:** 1x PD for `postgres-0` in `us-central1-a`
- **Teardown Command:**
  ```bash
  bash docs-exploration/runbook-deployments/instance1/teardown.sh
  ```

---

## Cost-Relevant Resources Created

- 2x `c3-standard-4` Compute Engine instances & boot disks in `us-central1-a` (~$0.42/hr)
- GKE zonal cluster management fee (~$0.10/hr)
- 1x 10Gi Standard PD for PostgreSQL
- GCS bucket storage in `us-central1`
- Container images in Artifact Registry / Container Registry
