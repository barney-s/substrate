PLANNED

## Instance Configuration & Resources to Create

- **Instance Name:** `instance1`
- **Resource Prefix:** `substrat-instance1`
- **Project ID:** `barni-cnrm-20260529` (Project Number: `77658989016`)
- **Region / Location:** `us-central1` / `us-central1-a`
- **GKE Cluster:** `substrat-instance1` (GKE Version `1.36.4-gke.1391000`, Node Pool `substrate-node-pool` with 2x `c3-standard-4` nodes, Kubernetes beta APIs enabled)
- **GCS Snapshot Bucket:** `gs://substrat-instance1-snap-77658989016` (Location: `us-central1`, Uniform Bucket-Level Access enabled)
- **Container Image Repository:** `gcr.io/barni-cnrm-20260529/substrat-instance1`
- **Kubectl Context:** `gke_barni-cnrm-20260529_us-central1-a_substrat-instance1`
- **Resource Label:** `repo-agent-instance=substrat-instance1`

## Estimated Costs

- **GKE Cluster & Compute:** 2x `c3-standard-4` VMs (~$0.42/hr total) + boot disks (~$0.08/day) + GKE cluster management fee (~$0.10/hr if not covered by free tier credits).
- **Persistent Disks:** 1 small PD for in-cluster PostgreSQL (~$0.04/day).
- **Storage & Registry:** GCS snapshot storage and Artifact Registry image layers (~<$0.05/day).
- **Teardown Duration:** ~10 minutes, cleanly drops all instance resources (GKE cluster, GCS snapshot bucket, images).

## Feasibility Checklist

- ✓ `serviceusage.services.enable` — verified for `tools/setup-gcp enable apis`
- ✓ `container.clusters.*` and `container.operations.get` — verified for cluster creation and node-pool configuration
- ✓ `iam.serviceAccounts.actAs` — compute SA `77658989016-compute@developer.gserviceaccount.com` verified
- ✓ `compute.networks.get`, `compute.subnetworks.use` — VPC `default` / Subnet `default` verified in `us-central1`
- ✓ `resourcemanager.projects.getIamPolicy/setIamPolicy` — verified for IAM role bindings
- ✓ `storage.buckets.create/delete` — verified for `gs://substrat-instance1-snap-77658989016`
- ✓ `artifactregistry.repositories.uploadArtifacts/downloadArtifacts` — `gcr.io` repo verified in `us`
- ✓ ADC authenticated via GKE metadata server
- ✓ Quota in `us-central1`: C3_CPUS and CPUS limits verified ample
- ✓ GKE 1.36 version `1.36.4-gke.1391000` verified available in `us-central1-a`
- ✓ Tooling: `go`, `gcloud`, `kubectl`, `gke-gcloud-auth-plugin`, `jq`, `git`, `make` all verified present

## Notes for Deployer

- `deploy.sh` provisions the GKE cluster with required beta APIs (`podcertificaterequests`, `clustertrustbundles`) via `tools/setup-gcp create cluster`, creates the snapshot bucket and IAM policy bindings, disables auto-upgrade on `substrate-node-pool`, deploys the Substrate control plane via `hack/install-ate.sh --deploy-ate-system`, and deploys the counter demo.
- No build or execution actions have been run during this planning phase.
